# AI-Powered Analytics Dashboard

An intelligent system that programmatically generates tailored Power BI dashboards (PBIP format) based on natural language requests using the Google Gemini API.

## ✨ Features

- **8-Page Premium Analytics UI** — Executive Overview, AI Builder, Trends, Geography, Products, Metadata Explorer, Export Center, Architecture
- **25+ Interactive Plotly Charts** — Line, area, bar, donut, treemap, heatmaps, Pareto, box plots, rolling averages, MoM growth
- **AI Dashboard Builder** — Natural language → Parsed Spec → Grounding Verification → Deterministic PBIP Generation
- **Global Filters** — Year-Month and Country multiselect with Top N slider; all charts respond to filter state
- **Premium Dark Theme** — Custom CSS with gradient hero banners, glassmorphic KPI cards, status badges, and polished typography
- **Deterministic Template Builder** — Power BI artifacts generated by template manipulation, never LLM-generated internals
- **Grounding & Validation** — Pydantic-validated specs, metadata-constrained LLM, full grounding transparency
- **Export Center** — Build history, artifact inspection, file tree viewer, and download support

## Architecture

This project strictly adheres to a deterministic modification approach, separating the AI reasoning layer from the Dashboard generation layer.

1. **Analytics Layer:** Processes raw transaction data into performant KPI CSV tables (`data/processed/`).
2. **Metadata Extraction:** `allowed_values.py` extracts valid `YearMonth` and `Country` parameters to ground the LLM.
3. **LLM Engine (Gemini):** Translates user natural language queries into a strict JSON `DashboardSpec`.
4. **Dashboard Builder:** Reads the verified JSON spec and modifies existing objects (filters, titles) within a pre-built Power BI Master Template (`dashboard/template/`) map.
5. **Streamlit UI:** Premium multipage frontend for analytics exploration, AI orchestration, and artifact delivery.

## Prerequisites

- Python 3.9+
- A Google Gemini API Key

## Setup

1. **Install Dependencies:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Environment Variables:**
   Create a `.env` file in the root directory:
   ```env
   GOOGLE_API_KEY="your_gemini_api_key_here"
   ```

3. **Prepare Data (First Time Only):**
   Ensure raw data is in `data/raw/online_retail_II.csv`. Run the analytics pipeline:
   ```bash
   python analytics/cleaning_v1.py
   python analytics/monthly_trends.py
   python analytics/country_contribution.py
   python analytics/top_products.py
   python analytics/month_country.py
   python analytics/month_product.py
   python analytics/allowed_values.py
   ```

## How to Run

```bash
streamlit run streamlit_app.py
```

The app opens at `http://localhost:8501` with 8 pages accessible via sidebar navigation.

## Project Structure

```
ai-powered-analytics/
├── streamlit_app.py           # Main entry point (sidebar + navigation)
├── pages/                     # 8 Streamlit pages
│   ├── 1_📊_Overview.py       # Executive dashboard with KPIs + charts
│   ├── 2_🤖_AI_Builder.py     # NL query → spec → grounding → build
│   ├── 3_📈_Trends.py         # Temporal analysis + rolling averages
│   ├── 4_🌍_Geography.py      # Country ranking + drill-down
│   ├── 5_📦_Products.py       # Product insights + Pareto
│   ├── 6_🔍_Metadata.py       # Grounding & metadata browser
│   ├── 7_📁_Export.py          # Build history & artifacts
│   └── 8_🏗️_Architecture.py   # System explainability
├── services/                  # Backend service layer
│   ├── data_loader.py         # Cached data loading
│   ├── metrics.py             # KPI computation + formatting
│   ├── ai_engine.py           # Gemini wrapper + grounding
│   └── builder.py             # PBIP template builder
├── ui/                        # UI component layer
│   ├── styles.py              # Custom CSS (dark theme)
│   ├── components.py          # Reusable UI helpers
│   └── charts.py              # 20+ Plotly chart functions
├── analytics/                 # Data processing pipeline
├── llm_engine/                # LLM client + prompts
├── dashboard/                 # PBIP spec + template + output
├── data/                      # Raw + processed datasets
├── .streamlit/config.toml     # Dark theme configuration
└── requirements.txt           # Python dependencies
```

## Dependencies

- `streamlit` — Multipage app framework
- `plotly` — Interactive chart library
- `pandas` — Data processing
- `pydantic` — Schema validation
- `google-generativeai` — Gemini API
- `python-dotenv` — Environment variables

## Constraints

- **Granularity:** Supported down to the Monthly level (YYYY-MM). Daily filtering is not supported in this version.
- **Security:** Raw transaction data is NEVER sent to Google. Only predefined capabilities and high-level extracted dimension lists (Allowed YearMonths, Countries) are provided in the prompt context.
